{
  "title": "Secure Linux Service Baseline",
  "description": "Production-grade systemd service with externalized secrets, reload, and health checks",
  "backend": {
    "imageid": "ubuntu"
  },
  "setup": {
    "commands": [
      "cp app.py /root/app.py",
      "chmod +x /root/app.py"
    ]
  }
}
