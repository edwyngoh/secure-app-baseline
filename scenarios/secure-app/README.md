# Secure Linux Service (Baseline)

In this scenario you will explore a production-grade Linux service that:

- Runs as a non-root user
- Loads secrets from the system securely
- Supports hot reload via SIGHUP
- Exposes a local health endpoint
- Shuts down gracefully under systemd

This scenario contains no real secrets.
