# Secure Linux Service Baseline

This lab is run in KillerCoda.  
Nothing is preinstalled. You must type each command yourself.

---

## Step 1: Create the application

Run the following command in the terminal to create `app.py`.

```bash
cat <<'EOF' > app.py
print("Hello from Secure App")
EOF
